<%= heading("Hello, " + current_user.username + "!") %>

<div class="page-header">
	<h2>あなたのカード一覧</h2>
</div>

<table class="table table-striped">
<%= showCardHeader() %>
<% @cards.each do |card| %>
<%= showCard(card) %>
<% end %>
</table>